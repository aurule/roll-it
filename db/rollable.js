const { db } = require("./db")

// api needs
// * class that saves the guildId
// * get rollable name and ID pairs for every table in a guild
// * get count of rollables for a guild
// guild class needs
// * create(name, desc, contents array)
// * get name and description pairs for all tables, maybe with counts
// * get count for id
// * get result for id and line number
// * update name, description, or contents
// schema
// * guildId
// * name
// * description
// * die (derived when contents changes)
// * contents: json array

module.exports = {
  //
}
