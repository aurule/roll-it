jest.mock("../../util/message-builders")

const starting = require("./starting")

describe("install starting message", () => {
  describe("data", () => {
    it.todo("shows the installed systems")
    it.todo("shows the installed features")
    it.todo("shows the installed commands")
    it.todo("shows the global commands")
    it.todo("shows systems button")
    it.todo("shows features button")
    it.todo("shows cancel button")
  })
})
