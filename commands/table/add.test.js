const table_add_command = require("./add")

describe("execute", () => {
  it.todo("warns on name collision")
  it.todo("warns on bad attachment type")
  it.todo("warns on short contents")
  it.todo("adds a table for the interaction's guild")
  it.todo("announces the new table")
  it.todo("announcement is ephemeral if quiet")
})
