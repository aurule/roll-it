const table_roll_command = require("./roll")

describe("execute", () => {
  it.todo("warns on missing table")
  it.todo("rolls a single result")
  it.todo("rolls multiple results")
  it.todo("shows the description, if present")
  it.todo("is ephemeral when secret")
})

describe("autocomplete", () => {
  it.todo("gets matching table names")
})
