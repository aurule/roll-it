const feedback_help_command = require("./feedback")

const { Interaction } = require("../../testing/interaction")

describe("execute", () => {
  describe("with a banned user", () => {
    it.todo("does not add feedback")
  })

  describe("with a valid user", () => {
    it.todo("saves message")

    it.todo("saves command")

    it.todo("saves consent")

    it.todo("replies with a success message")
  })
})
